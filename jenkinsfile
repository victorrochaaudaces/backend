pipeline {
    agent TI audaces

    stages {
        stage ('Build Image') {
            steps {
                script {
                    dockerapp = docker.Build("victorrochaaudaces/teste-node", '-f ./dockerfile ./')
                }
            }
        }
    }
}