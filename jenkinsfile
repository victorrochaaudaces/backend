pipeline {
    agent any

    stages {
        stage ('Build Image') {
            steps {
                script {
                    dockerapp = docker.build("victorrochaaudaces/teste-node", '-f ./src/dockerfile ./src')
                }
            }
        }
    }
}